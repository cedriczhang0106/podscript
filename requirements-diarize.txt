# 可选：说话人分离（访谈类播客分清谁在说）。基础转写用不到这些，按需安装：
#   pip install -r requirements-diarize.txt
# 之后还需： ① hf.co/settings/tokens 建 token ② 接受 pyannote/speaker-diarization-3.1 许可
#           ③ export HF_TOKEN=hf_xxx
pyannote.audio>=3.1
torch>=2.0
torchaudio>=2.0
httpx[socks]   # 走 SOCKS 代理（如 Clash）下模型时需要；不走代理可忽略
